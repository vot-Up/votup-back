---
status: pending
title: Remove reversion from viewset and add simple-history to models
type: refactor
complexity: medium
dependencies:
- task_01
---

# Task 02: Remove reversion from viewset and add simple-history to models

## Overview
Remove django-reversion usage from `core/viewset.py` (ViewSetBase uses `revisions.create_revision()` on create/update) and replace it with django-simple-history's automatic model-level tracking. Add `HistoricalRecords()` to all 9 business models and the custom User model, then create and run migrations.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- MUST remove `from reversion import revisions` import from core/viewset.py
- MUST remove `revisions.create_revision()`, `revisions.set_user()`, `revisions.set_comment()` calls from ViewSetBase.create() and ViewSetBase.update()
- MUST keep the `transaction.atomic()` context manager in create/update methods (reversion's create_revision was nested inside it)
- MUST keep the logger.info() calls in create/update methods
- MUST add `from simple_history.models import HistoricalRecords` to core/models/models.py and account/models.py
- MUST add `history = HistoricalRecords()` to all 8 business models: Voter, Candidate, Plate, PlateUser, EventVoting, VotingPlate, VotingUser, ResumeVote
- MUST add `history = HistoricalRecords()` to the User model in account/models.py
- MUST run `uv run python manage.py makemigrations` and `uv run python manage.py migrate` successfully
- MUST verify all existing 36 tests still pass after changes
</requirements>

## Subtasks
- [ ] 2.1 Remove reversion import and context manager usage from core/viewset.py (keep transaction.atomic and logger)
- [ ] 2.2 Add HistoricalRecords to all 8 business models in core/models/models.py
- [ ] 2.3 Add HistoricalRecords to User model in account/models.py
- [ ] 2.4 Run `manage.py makemigrations` to create history table migrations
- [ ] 2.5 Run `manage.py migrate` to apply migrations
- [ ] 2.6 Run all existing tests to confirm no regressions

## Implementation Details
See TechSpec "Data Models" section for HistoricalRecords addition. The key insight is that simple-history tracks changes automatically at the model level via signals — no manual `create_revision()` calls needed in the viewset. The ViewSetBase.create() and update() methods should simplify to just `with transaction.atomic():` + `logger.info()` + `super()` call. ModelBase (abstract base in account/models.py) should NOT get HistoricalRecords — each concrete model gets its own.

### Relevant Files
- `core/viewset.py` — lines 11 (import), 55-57 (create revision), 62-64 (update revision)
- `core/models/models.py` — all 8 model classes
- `account/models.py` — User model class

### Dependent Files
- `core/viewset.py` — reversion context managers removed
- `core/models/models.py` — HistoricalRecords added per model
- `account/models.py` — HistoricalRecords added to User
- Migration files — auto-generated by makemigrations

### Related ADRs
- [ADR-004: Version History with django-simple-history](../adrs/adr-004.md) — Replace reversion with simple-history

## Deliverables
- Updated core/viewset.py without reversion imports/calls
- Updated core/models/models.py with HistoricalRecords on all 8 models
- Updated account/models.py with HistoricalRecords on User
- New migration files for history tables
- All existing 36 tests pass
- Unit tests for viewset create/update behavior without reversion

## Tests
- Unit tests:
  - [ ] ViewSetBase.create() still wraps in transaction.atomic() (verify no reversion context)
  - [ ] ViewSetBase.update() still wraps in transaction.atomic() (verify no reversion context)
  - [ ] Each model has a `history` attribute (HistoricalRecords)
  - [ ] Creating a model instance via ORM creates a historical record
- Integration tests:
  - [ ] `manage.py makemigrations --check` shows no pending migrations
  - [ ] `manage.py migrate` runs without errors
  - [ ] All 36 existing tests pass
- Test coverage target: >=80%

## Success Criteria
- All tests passing
- Test coverage >=80%
- `manage.py check` passes
- `manage.py migrate` succeeds
- No reversion imports remain in the codebase
